import 'package:flutter/material.dart'; import '../main.dart'; class SettingsScreen extends StatelessWidget { @override Widget build(BuildContext context){ return WatermarkScaffold(title: 'الإعدادات', child: ListView(children: [ListTile(title: Text('الثيم'), subtitle: Text('فاتح / غامق')), ListTile(title: Text('الوان التطبيق')), Divider(), Center(child: Padding(padding: EdgeInsets.all(20), child: Text('جميع الحقوق محفوظة - أبو بحري 2026', style: TextStyle(fontWeight: FontWeight.bold))))])); } }