import 'package:flutter/material.dart'; import '../main.dart'; class QuranScreen extends StatelessWidget { final surahs=['الفاتحة','البقرة','آل عمران','النساء','المائدة','الأنعام','الأعراف','الأنفال','التوبة','يونس','هود','يوسف']; @override Widget build(BuildContext context){ return WatermarkScaffold(title: 'القرآن الكريم - نص وصوت', child: ListView.builder(itemCount: surahs.length, itemBuilder: (c,i)=>ListTile(leading: CircleAvatar(child: Text('\${i+1}')), title: Text(surahs[i]), trailing: Icon(Icons.play_arrow), onTap: (){}))); } }